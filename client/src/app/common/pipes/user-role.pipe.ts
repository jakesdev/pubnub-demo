// import {Pipe, PipeTransform} from '@angular/core';
// import {USER_SOURCE_ROLES} from '@app/shared/app.constants';

// @Pipe({
//     name: 'userRole',
//     standalone: true,
// })
// export class UserRolePipe implements PipeTransform {

//     transform(value: any): any {
//         const role = USER_SOURCE_ROLES.find(_ => _.value === value);
//         if (!!role) {
//             return role.key;
//         }

//         return value;
//     }

// }
